---
description: Secret-handling rule
alwaysApply: true
---

# Secrets

Never read, edit, create, display, or commit real `.env*`, credentials, tokens, keys, or passwords. Use `.env.example` and configuration documentation for names only. If a secret is needed, ask the user to set it locally; never request it in chat. If exposed accidentally, do not repeat it and recommend rotation.
